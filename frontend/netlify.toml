[build]
  base = "frontend"
  publish = "dist"
  command = "npm install && npm run build"

[functions]
  directory = "netlify-functions"
  timeout = 10

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
